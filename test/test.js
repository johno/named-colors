var assert = require('assert');
var namedColors = require('..');

describe('named-colors', function() {

  it('should do something awesome', function() {
    assert.equal(namedColors(), true);
  });
});
